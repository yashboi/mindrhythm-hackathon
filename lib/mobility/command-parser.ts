import { mockLocations } from "@/lib/data/mock-locations";
export function parseMobilityCommand(command:string){const q=command.toLowerCase();const locations=mockLocations.filter(l=>q.includes(l.name.toLowerCase())||q.includes(l.id.replaceAll("-"," "))).map(l=>l.name);let intent:"compare"|"scan"|"show"|"rank"|"report"|"unknown"="unknown";if(q.includes("compare"))intent="compare";else if(q.includes("scan"))intent="scan";else if(q.includes("rank"))intent="rank";else if(q.includes("report"))intent="report";else if(q.includes("show")||q.includes("explain"))intent="show";const metric=["rent burden","mobility yield","mobility score","displacement risk","housing permits","job access","commute"].find(m=>q.includes(m));return {intent,locations,metric};}
