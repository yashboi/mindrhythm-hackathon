import { mockLocations } from "./mock-locations";
import type { TimeSeriesPoint } from "@/lib/mobility/types";
export const mockTimeSeries: Record<string, TimeSeriesPoint[]> = Object.fromEntries(mockLocations.map((l,idx)=>[l.id,Array.from({length:9},(_,i)=>{const year=2018+i; const wave=Math.round(Math.sin((i+idx)*.9)*2); return {year,rentBurden:Math.max(22, l.rentBurden-5+i*.7+wave),medianIncome:Math.round(l.medianIncome*(.82+i*.025)+(wave*700)),commuteBurdenMinutes:Math.max(18,l.commuteBurdenMinutes-2+i*.28-wave*.4),housingPermits:Math.max(80,Math.round(l.housingProductionIndex*7+(i%3)*45+wave*16+idx*9)),mobilityScore:Math.max(30,Math.min(92,l.mobilityScore- (l.mobilityTrend==="improving"?4:-3)+i*(l.mobilityTrend==="improving"?.8:l.mobilityTrend==="declining"?-.55:.15)+wave*.4)),displacementRiskScore:Math.max(15,Math.min(98,l.displacementRiskScore-3+i*(l.riskLevel==="critical"?1.1:l.riskLevel==="high"?.55:.15)+wave))};})]));
