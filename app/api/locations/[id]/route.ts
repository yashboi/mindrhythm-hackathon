import { NextResponse } from "next/server";import { findLocation } from "@/lib/data/mock-locations";import { mockTimeSeries } from "@/lib/data/mock-timeseries";import { mockSources } from "@/lib/data/mock-sources";
export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const location=findLocation(id);if(!location)return NextResponse.json({error:"NO DATA SIGNAL FOUND",detail:"Try a different location, metric, or command."},{status:404});return NextResponse.json({location,timeSeries:mockTimeSeries[location.id]??[],sources:mockSources})}
